# -*- mode: snippet -*-
# name: Selected Fraction auto
# binding : C-`
# condition: (texmathp)
# group: math
# --
\\frac{`(if (region-active-p) (replace-regexp-in-string "^(\\|)$" "" (yas-selected-text)))` $1}{$0}
